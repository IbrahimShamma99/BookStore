const path = require("path");
const { paths } = require("../helper/");

//SECTION Add here exported Dirs for you client side app
const Dirs = [paths.publicDir, paths.userAvatar, paths.bookCover];

module.exports = Dirs;
